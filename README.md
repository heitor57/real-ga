# Usage

```sh
python genetic_algorithm.py --help
usage: genetic_algorithm.py [-h] [--num_pop NUM_POP] [--num_generations NUM_GENERATIONS] [--num_bits NUM_BITS] [--num_genes NUM_GENES] [--cross_rate CROSS_RATE]
                            [--elitism_rate ELITISM_RATE] [--mutateds_rate MUTATEDS_RATE] [--mutation_rate MUTATION_RATE] [--eid EID]

optional arguments:
  -h, --help            show this help message and exit
  --num_pop NUM_POP
  --num_generations NUM_GENERATIONS
  --num_bits NUM_BITS
  --num_genes NUM_GENES
  --cross_rate CROSS_RATE
  --elitism_rate ELITISM_RATE
  --mutateds_rate MUTATEDS_RATE
  --mutation_rate MUTATION_RATE
  --eid EID
´´´
